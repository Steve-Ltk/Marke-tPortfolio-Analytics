@if (TempData["SuccessMessage"] != null)
{
    <div class="mpa-alert mpa-alert-success" style="margin:0 36px 16px">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20,6 9,17 4,12" /></svg>
        @TempData["SuccessMessage"]
        <button class="alert-close" onclick="this.closest('.mpa-alert').remove()">✕</button>
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="mpa-alert mpa-alert-error" style="margin:0 36px 16px">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10" /><line x1="12" y1="8" x2="12" y2="12" /><line x1="12" y1="16" x2="12.01" y2="16" /></svg>
        @TempData["ErrorMessage"]
        <button class="alert-close" onclick="this.closest('.mpa-alert').remove()">✕</button>
    </div>
}
@if (TempData["WarningMessage"] != null)
{
    <div class="mpa-alert mpa-alert-warning" style="margin:0 36px 16px">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" /><line x1="12" y1="9" x2="12" y2="13" /><line x1="12" y1="17" x2="12.01" y2="17" /></svg>
        @TempData["WarningMessage"]
        <button class="alert-close" onclick="this.closest('.mpa-alert').remove()">✕</button>
    </div>
}
